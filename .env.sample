# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Configuration
# Format: mongodb://[username:password@]host[:port][/[database][?options]]
MONGO_URL=mongodb://localhost:27017/task-manager

# JWT Configuration
# Generate a secure random string for JWT_SECRET
JWT_SECRET=replace_with_secure_random_string
JWT_EXPIRES_IN=7d

# Email Configuration (for nodemailer)
# For Gmail, you'll need to use an App Password
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_specific_password
SMTP_FROM=your_email@gmail.com

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# Cookie Configuration
# Generate a secure random string for COOKIE_SECRET
COOKIE_SECRET=replace_with_secure_random_string
COOKIE_EXPIRES_IN=7d

# Instructions:
# 1. Copy this file to .env
# 2. Replace all placeholder values with your actual configuration
# 3. For security-sensitive values (JWT_SECRET, COOKIE_SECRET), use strong random strings
# 4. For Gmail SMTP, you'll need to:
#    - Enable 2-factor authentication
#    - Generate an App Password
#    - Use that App Password as SMTP_PASS
